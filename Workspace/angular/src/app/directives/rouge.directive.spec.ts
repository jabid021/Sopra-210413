import { RougeDirective } from './rouge.directive';

describe('RougeDirective', () => {
  it('should create an instance', () => {
    const directive = new RougeDirective();
    expect(directive).toBeTruthy();
  });
});
